<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <style name="OwnCloud" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Main theme colors -->
        <!-- App Bar -->
        <item name="colorPrimary">@color/primary</item>
        <!-- Status Bar and contextual Status Bars -->
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <!-- Snackbar Action Link -->
        <item name="colorAccent">@color/primary</item>
        <item name="android:buttonStyle">@style/ocbutton</item>
        <item name="android:windowTitleBackgroundStyle">@color/bg_normal</item>
    </style>

<<<<<<< HEAD
    <style name="SelectSingleNote" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:background">@color/bg_highlighted</item>
        <item name="android:textColor">@color/primary_dark</item>
        <item name="colorAccent">@color/primary</item>
        <item name="colorPrimaryDark">@color/bg_normal</item>
        <item name="colorPrimary">@color/cardview_dark_background</item>
        <item name="android:textColorPrimaryInverse">@color/primary</item>
        <item name="android:colorBackground">@color/bg_normal</item>
        <item name="android:windowBackground">@color/bg_normal</item>
        <item name="android:textColorPrimary">@color/primary</item>
=======
    <style name="SelectSingleNote">
        <item name="android:colorPrimary">@color/bg_highlighted</item>

>>>>>>> 7951365... .
    </style>

    <!--
        cert4android is using these styles and we want the same button style in the trust activity
    -->
    <style name="Widget.AppCompat.Button.Borderless" parent="ocbutton">
        <item name="android:layout_margin">2dp</item>
    </style>
    <style name="Widget.AppCompat.Button.Borderless.Colored" parent="Widget.AppCompat.Button.Borderless">
    </style>

    <style name="fab">
        <item name="android:layout_margin">16dp</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_gravity">end|bottom</item>
        <item name="android:layout_alignParentBottom">true</item>
        <item name="android:layout_alignParentRight">true</item>
    </style>

    <style name="ocbutton" parent="android:style/Widget.Button">
        <item name="android:background">@color/primary</item>
        <item name="android:textColor">@drawable/button_selector</item>
        <item name="android:padding">@dimen/button_padding</item>
        <item name="android:elevation">@dimen/button_elevation</item>
    </style>

    <style name="SplashTheme" parent="OwnCloud">
        <item name="android:windowBackground">@drawable/splashscreen</item>
    </style>
</resources>