# Local release signing config for Gradle (do not commit real secrets)
# Copy this file to "keystore.properties" in the repo root and fill in your values.

storeFile=/absolute/path/to/your/release.keystore
storePassword=change-me
keyAlias=notes-release
keyPassword=change-me
